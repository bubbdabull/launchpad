export type PlatformStats = {
  /** Live launches (mint window open on the launchpad). */
  launchesLive: number;
  /** Total NFT supply minted across all launches. */
  totalMinted: number;
  /** Total NFT supply across all launches. */
  totalSupply: number;
  /** Total launches. */
  totalLaunches: number;
};
