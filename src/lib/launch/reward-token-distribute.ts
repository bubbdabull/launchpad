/**
 * Token holder batch payouts were removed from the backend.
 *
 * SPL reward splits must be enforced on-chain (claim / stream program). This file
 * intentionally exports nothing for distribution planning — do not reintroduce
 * server-side allocation math here.
 */

export {};
